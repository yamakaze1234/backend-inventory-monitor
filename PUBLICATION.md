# 正式版 v1.0 公开发布说明

发布基线为本机正式版 v1.0。整理时核验原构建清单列出的 34 项源码与资源，均与工作区相符。

公开副本保留当前业务源码、Python 测试、ERP 采集测试、图标、使用说明与第三方许可。首次配置中的实际群名、人员名，以及相关测试数据，替换为示例名称；不修改原工作区和本机运行数据。

打包配置从临时目录移到 `packaging/`，依赖通过 requirements 文件声明，默认构建输出改到 `dist/`。Release EXE 由公开副本重新构建，因此其哈希与本机原始 EXE 不同。二者都使用正式版 v1.0 业务版本标识。

未收录历史构建、生产配置、库存数据库、聊天记录、旧验收脚本、其他项目、3cerp-js 或三个独立抖店油猴工具。

每个发布安装包附带 `build-manifest.json`，Release 附有 ZIP SHA-256 校验文件。

依赖本机来源配置的两项历史测试改为读取 tests/fixtures/ 中的虚构群、人员及标识，保证新检出仓库可独立验证。
