# 后台库存监控使用说明

适用版本：正式版 v1.0 · Windows

## 1. 下载与启动

1. 打开 [GitHub 下载页](https://github.com/yamakaze1234/backend-inventory-monitor/releases/latest)。
2. 在 Assets 中下载 `backend-inventory-monitor-v1.0-windows.zip`。`Source code` 是开发源码，日常使用下载 Windows 安装包即可。
3. 将 ZIP 全部解压到固定、可写的文件夹，例如 `D:/后台库存监控/`，不要在压缩包里直接运行。
4. 双击 `后台库存监控_正式版_v1.0.exe`。安装包已包含 Python 和钉钉命令组件。

电脑需要保持唤醒并联网。首次运行没有自己的产品和机器人配置，请按下面步骤设置。

## 2. 连接 ERP 库存采集

1. 在平时登录 ERP 的浏览器中，使用脚本猫或 Tampermonkey 安装随包的 `inventory-erp.user.js`。
2. 登录 `https://cqzs.3cerp.com/`，打开“库房管理 → 分库库存”，选择“公司大库”，保持该页打开。
3. 点击页面右下角“库存采集：点击连接”，输入 ERP 顶栏显示的账号姓名。
4. 保持桌面程序运行。浏览器显示“已更新”，桌面出现最近成功检查时间，即完成首次连接。

该脚本只适配对应 ERP 页面。库存数据通过本机 `127.0.0.1:18763` 传给桌面程序。它是独立采集脚本，不覆盖其他 ERP 辅助脚本。

## 3. 添加关注产品

1. 点击“＋ 添加产品”。
2. 在“搜索产品”选择“goodsid 精确搜索”或“名称模糊搜索”。goodsid 是“基础资料 → ERP 商品 → 编号”，与 SKU 编码不同。
3. 双击搜索结果或点击“选择并配置”。
4. 核对产品分类、库存依据和预警数量，保存。

默认按“公司大库可销数”判断。需要监控特定仓库时，改为“指定分库库存数”，刷新分库，选择真实仓库后保存。分库查询需要新版采集脚本和 ERP 在线。

“取消并返回搜索产品”会放弃未保存修改并回到搜索。删除关注只移除本地监控配置，不删除 ERP 商品。

## 4. 配置钉钉通知机器人

1. 准备接收群的钉钉自定义机器人 Webhook。
2. 打开“采集设置 → 通知机器人”，添加机器人并填写备注。
3. 输入自己的 Webhook，勾选需要接收的库存提醒、货源日报或货源消息即时转发。
4. 保存后检查启用状态。多个机器人可以分别接收不同类型的通知。

关键词按接收类型配置“库存”“货源情况”“价格调整”；当前不支持加签模式。机器人地址决定接收群，备注仅供识别。地址加密保存在本机，不在界面回显。

正式程序配置完成后会按规则发送通知。暂停机器人后该机器人停止接收新通知；恢复时不补发暂停期间积压。

## 5. 配置货源消息监听

1. 打开“钉钉监控”，使用自己的钉钉账号完成登录授权。
2. 将首次显示的示例群名和人员名替换成实际要监听的来源。
3. 核对当前账号、群、人员和关键词，再完成配置并启动。
4. 保持钉钉运行，检查界面中的各来源连接状态。

窗口打开或后台进程存在不等于来源已连接。若状态异常，先检查账号授权、群成员资格、钉钉运行状态和网络。

## 6. 生成日报、周报

1. 进入“货源汇报”，配置自己的 AI HTTPS 接口、模型名称和 Key。
2. 按界面选择报告周期并生成报告。
3. 在预览中检查内容、来源和日期。
4. 需要手动发送时，选择接收机器人，确认已检查内容，再执行发送。

库存监控本身不依赖 AI。AI 货源汇报需要有效接口配置、可用来源数据和网络；接口失败时不会自动生成替代报告。

## 7. 日常运行与退出

- 库存默认每 2 小时检查一次，可在采集设置调整；“立即检查”可提前检查。
- 关闭窗口会收起到托盘，后台监控继续运行。
- 完全退出时，从托盘选择“退出程序（同时停止钉钉监控）”。
- ERP 退出登录、关闭标签页或电脑休眠时，库存采集会暂停；恢复连接后补一次检查。
- “最近成功检查时间”表示已取得库存快照的时间；未知数量不会被当作零库存。

## 8. 升级并保留原数据

1. 从托盘完全退出旧程序。
2. 备份原程序目录及本机用户数据。
3. 将新版 EXE、采集脚本和说明复制到原程序所在文件夹。
4. 保留原目录 `.local_inventory_ui_test/`，尤其是其中的 `inventory.sqlite3`。
5. 保留 `%LOCALAPPDATA%/DingTalkSourceMonitor/`，其中保存钉钉及机器人相关本机数据。
6. 在浏览器更新采集脚本并刷新 ERP，再启动新版，核对产品配置和历史记录。

原工作目录名称 `.local_inventory_ui_test` 为兼容历史版本保留，也是正式程序的数据目录。给其他人使用时发送干净安装包，由对方自行配置。

## 9. 常见问题

| 现象 | 处理方法 |
| --- | --- |
| 提示等待 ERP | 检查 ERP 登录、分库库存页、公司大库选择、采集脚本启用和桌面程序运行状态 |
| 18763 端口被占用 | 从托盘退出其他库存监控实例，再启动一个实例 |
| 搜不到产品 | 先完成目录采集；goodsid 输入完整编号，名称可用空格分隔关键词 |
| 分库无法刷新 | 检查 ERP 在线，更新随包采集脚本后刷新页面；查询失败时不要把旧数值当作实时库存 |
| 库存数量显示待检查 | 最近查询不完整或数量不可用，恢复采集后检查，不代表库存为零 |
| 没收到库存提醒 | 检查是否触发阈值、机器人是否启用、接收类型和关键词是否匹配，再查看投递结果 |
| 提示发送待核实 | 请求结果不明确，程序不会自动重复发送；先核对接收群记录 |
| 钉钉来源离线 | 检查钉钉运行、登录授权、账号是否在目标群及网络情况 |
| AI 报告生成失败 | 检查接口、模型、Key、额度、网络及所选周期是否有来源数据 |
| 关窗口后仍在运行 | 这是托盘运行方式，从托盘菜单完全退出 |

库存、入库及分库口径的详细解释见同目录 `inventory-setup.md`。
