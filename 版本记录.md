# v1.04 · 2026-09-17

- 独立监测缺少分库记录商品的 ERP 总待入，显示并播报“仓库未确认”。
- 新维度首次建立基线，来源切换防重复，不将缺失库存当作零。
- 保留原仓库、阈值、待入提醒开关及机器人接收配置。

# v1.03 · 2026-09-17

- SQL Server 默认查询，完整商品目录、仓库选择、待入及库存净增加提醒，保留脚本采集选项。
- 切换来源首轮建立基线；不将缺失仓库记录视为零，不使用总量替代所选仓库。
- 排除停售、套餐和名称含星号的新增关注产品；库存提醒记录筛选与导出。
- SQL 驱动随包提供；数据库连接自行配置，密码可选择当前 Windows 用户加密保存。
- 机器人与播报格式沿用原有实现；同步日报纯到货解析和生成失败有限重试修复。

# v1.02 · 2026-09-14

- Excel / CSV 导入监控产品，goodsid 精确匹配，统一公司大库，确认总览后保存。
- 批量勾选 / 全选删除关注，保留历史记录。
- 普通货源消息标题修正，界面统一。

# 库存监控版本记录

## 正式版 v1.0（2026-09-11）

沿用迭代版全部功能与原有数据，统一正式版命名。窗口、右上角、EXE、打包和桌面入口使用同一版本来源 inventory_version.py。以下迭代号仅保留作历史记录。


开发文件始终在当前项目目录迭代。历史源码使用 Git 提交保存，不为每次修改建立新目录。已有交付文件保留，用户确认前不生成新的 EXE。

用户指定的固定程序目录：`outputs/钉钉库存联动`。运行 `pwsh.exe -File 更新本地测试版.ps1` 更新该目录内的本地测试源码，不覆盖测试数据库。以后更新均使用此目录。双击目录内“启动本地测试.cmd”启动。

## 1.7.2 — 滚动响应优化（2026-09-11）

- 设置和汇报页按滚轮输入幅度滚动，保留不足一像素的余量；列表保留高精度滚轮输入，避免小幅输入丢失。
- 滚动条复用滑块，圆角卡片尺寸不变时不重绘。
- 库存与钉钉列表只更新变化行，保留选中项与滚动位置；库存动态内容不变时不重写，隐藏的钉钉页暂停刷新。
- 运行中的旧 EXE 不会自动加载源码更新；退出旧程序后启动固定目录内 v1.7.2。

## 1.4.0 — 产品级分库库存特例（2026-09-11）

- 产品配置新增“预警依据”和“指定分库”，从该产品总库存点开的分库数查询读取选项；样品库可选，未选中的库不计入。
- 分库库存使用独立预警数量，原始公司大库可销和库存字段不被替换；负分库库存显示“负库存”。
- 分库查询失败不回退为零或公司大库数量；切换依据取消尚未开始发送的旧口径提醒，旧采集任务会让位于新配置检查。
- 稀缺货源 20、样品库 1 的实时只读数据已通过隔离服务与实际 Tk 界面验收；保存、取消、重新读取及小窗口均已验证，未发送消息。
- 程序为后台库存监控_v1.4.0.exe，配套库存采集脚本升级为 0.2.0。旧程序和浏览器已安装脚本需要切换/更新后才会使用此功能。

## 1.3.0 — 三类通知多机器人（2026-09-11）

- 库存提醒、货源日报、钉钉货源消息即时转发共用机器人管理，支持添加、编辑、删除和单独暂停/恢复。
- 每个机器人可选择接收类型，名称仅作备注，目标群由 Webhook 决定；同一机器人同类型重复配置会被拦截。
- 逐机器人保存投递结果；失败不影响其他接收方，成功或结果未知的不自动重发。暂停期间消息不补发，已开始发送的请求可能仍送达。
- 旧库存/日报与货源消息配置按原范围迁移，空日报仍跳过；本机加密配置不进入分发包。
- 程序：后台库存监控_v1.3.0.exe；部门分发包：库存监控 v1.3.0.zip。

## 1.2.2 — 空货源日报不发送（2026-09-11）

- 截至发送时无可汇总消息、只有空白或排除内容、AI 未提取到明确产品时，本时段标记 skipped，不调用机器人发送。
- 已跳过的时段不重复执行；下一时段仍照常查询和汇报。采集异常仍标为 failed。
- 最新程序：后台库存监控_v1.2.2.exe；部门分发包：库存监控 v1.2.2.zip。
- 49 项相关测试及打包 EXE 六页/向导离线启动检查通过。

## 1.2.1 — 退出时停止钉钉监控（2026-09-11）

- 托盘“退出程序（同时停止钉钉监控）”先发出停止请求，确认监听子进程停止后再退出库存程序。
- 停止等待在后台执行；超时或失败时保留程序并提示重试，不把未停止误报为退出成功。
- 右上角关闭按钮仍收起到托盘。重新启用钉钉监控可在该功能页点击“启动钉钉监控”。
- 新入口：`outputs/钉钉库存联动/后台库存监控_v1.2.1.exe`。26 项退出、监控控制和界面相关测试通过。

## 1.2 — 圆角界面统一（2026-09-11）

- 最新程序：`outputs/钉钉库存联动/后台库存监控_v1.2.exe`，桌面快捷方式同步更新，旧版保留。
- 根据用户确认的参考图，统一蓝色圆角按钮、输入框、卡片、列表和滚动条，涵盖库存、产品、采集、日报 AI、钉钉和配置向导。
- 修复小窗口库存列表被动态区挤掉的问题；最近动态支持展开，长页面支持滚轮，表格补齐横向滚动。
- 新增共享 inventory_theme.py；向导初始化不再覆盖主窗口主题。
- 83 项相关测试、19 项设计检查及打包 EXE 六页/向导离线启动验证通过。未使用真实群推送验证。
- 原库存、提醒、日报与消息业务规则沿用。详见固定目录 `v1.2-界面更新说明.md`。

## 1.1 界面更新 — 返回搜索产品（2026-09-11）

- 右下角“取消”改为“取消并返回”：撤销未保存的配置并返回搜索页，保留搜索条件和结果；新增及编辑两种场景均已通过实际按钮验证。
- 最新程序：`outputs/钉钉库存联动/后台库存监控_v1.1_取消并返回.exe`。
- 产品配置页顶部新增“← 返回搜索产品”，滚动配置表单时仍保持可见。
- 返回后保留搜索模式、关键词和匹配结果，并聚焦搜索框，可继续选择下一件产品；返回操作不自动保存配置。
- 29 项相关测试及实际窗口验证通过，另已验证保存后返回、连续选品和最小窗口尺寸下的按钮可见性。
- 更新程序：`outputs/钉钉库存联动/后台库存监控_v1.1_返回搜索.exe`；已打开的旧程序需退出后打开更新程序。

## 1.1 — 库存与钉钉监控统一工作台（2026-09-10）

- 固定入口：`outputs/钉钉库存联动/库存与钉钉监控_v1.1.exe`，原 v1.0 保留。
- 左侧新增独立“钉钉监控”页面，显示监控人员、来源群、连接状态，提供启动、停止、一键配置。
- 沿用当前用户已安装钉钉监控的数据目录，不复制凭据、不重建消息来源、不新增重复订阅。库存仍沿用原配置目录。
- 此次整合统一桌面控制入口，现有消息监听进程继续运行；两类监控的启停与通知配置分别保留。
- 75 项相关测试和真实窗口验证通过；整合 EXE 已启动，现有钉钉来源 4/4 仍连接。

## 1.0 — 正式运行版（2026-09-10）

- 固定入口：`outputs/钉钉库存联动/钉钉库存联动_v1.0.exe`。
- 启用指定群机器人自动通知，沿用重点产品、加密机器人配置及监控周期。
- 首次升级取消测试积压事件并保存升级前快照，立即请求新采集，以新状态生成提醒；升级动作只执行一次。
- 旧测试入口保留用于调试，正式使用打开 v1.0 EXE。不能同时占用采集端口。
- 56 项相关测试通过；EXE 已实际启动，采集端口及配置保留已验证。
- ERP 浏览器必须保持登录和采集脚本运行；正式上线时浏览器暂未回传新快照，真实自动投递等待下一次采集触发。

## 0.9 — 库存增加及待入库提醒（2026-09-10）

- 实际库存增加产生提醒；同轮有新入库单时只保留确认入库提醒。
- 待入首次为正数、从零恢复或继续增加产生提醒，相同数量及减少不重复提醒。
- 产品配置增加待入库开关；旧配置默认开启。提醒不展示 SKU，使用蓝色库存增加及紫色待入库标识。
- 48 项相关测试及实际窗口验证通过。本地联调版仍不启动自动发送，本次未向群发送消息。

## 0.8 — 可配置监控周期（2026-09-10）

- 采集设置新增监控周期，按分钟输入，默认 120，支持 1–10080 分钟。
- 保存后重算下次检查时间，按最后成功采集时间加新周期计算；已到期则等待下一次浏览器心跳采集。
- 重启保留设置，暂停状态不被修改；界面和桥接使用同一周期。
- 23 项规则和桥接测试、实际窗口验证通过。

## 0.7.1 — 库存消息排版（2026-09-10）

- 自动提醒和手动检测信息均不展示 SKU，只保留商品名称和 goodsid（商品编号）。
- 状态使用彩色色块：🟧 低库存、🟥 超售、🟩 正常或恢复、🟦 入库。
- 可销库存与预警值加粗，实际库存、待入库及采集时间分行展示；超售排在低库存之前。
- 后续手动检测消息使用 `inventory_delivery.format_inventory_report`，自动提醒使用 `format_events`，不再临时拼接 SKU 字段。
- 20 项发送与格式相关测试通过；本次不发送消息。

## 0.7 — 库存通知改用机器人（2026-09-10）

- 正式库存发送服务仅创建 RobotSender，不再使用用户身份发送器，也不回退。
- 采集设置提供示例库存通知群机器人 Webhook 的掩码输入与 Windows 用户绑定加密保存。
- 缺少配置不发送；成功按机器人 errcode=0 判断，网络异常保留未知结果。
- 本地 ERP 联调版继续关闭发送服务。本次仅使用模拟回执验证，没有实际发送。

## 0.6 — 工作区独立页面（2026-09-10）

- 库存监控仅显示关注列表和库存动态；添加产品仅显示搜索；选择结果后进入独立配置页。
- 采集设置使用独立说明页。导航同步更新标题及选中状态。
- 关注列表单击只选择，双击或“编辑配置”打开编辑。实际窗口验证各页面只显示对应模块。

## 0.5 — 深色侧栏与卡片工作台（2026-09-10）

- 按用户参考图改为深色左侧导航、顶部面包屑、浅灰工作区、白色卡片及紫蓝操作按钮。
- 侧栏提供库存监控、添加产品、采集设置和立即检查入口。
- 沿用 ERP 采集桥接、goodsid 搜索及重点产品配置；实际窗口保存、删除、搜索和缩放验证通过。

## 0.4 — 本地版连接 ERP 实时采集（2026-09-10）

- 启动时自动开启本机 127.0.0.1:18763 采集桥接，关闭窗口时释放端口。
- 沿用本地测试的重点产品配置，接收公司大库实时采集，不启动钉钉发送服务。
- 历史目录仅用于空数据库初始化，不覆盖新采集记录。
- 浏览器需启用随附采集脚本，并保持已登录的“分库库存 → 公司大库”页面打开。

## 0.3 — 删除关注产品（2026-09-09）

- 选中左侧重点产品后点击“删除关注”，移除该产品的监控配置。
- 已移除配置留档，取消待发送及待重试提醒；已进入发送中的通知可能仍会送达。
- ERP 采集目录保留，可重新搜索添加。实际窗口删除、重启后状态及不再发送的测试通过。

## 0.2 — goodsid 搜索与添加区排版（2026-09-09）

- goodsid 使用完整采集记录中的 goods_id，对应基础资料 → ERP 商品 → 编号；与 SKU 编码分别展示和保存。
- goodsid 精确搜索、名称多关键词模糊搜索，搜索覆盖完整商品目录。
- 左侧重点产品约占 40%，右侧搜索及配置约占 60%；右侧使用两个页签，增大搜索输入框。
- 旧配置根据采集目录补齐 goodsid；保存时拒绝与已知商品不一致的编号。
- 本地测试入口：`py -3 test_inventory_ui_local.py`。测试数据独立保存，不发送钉钉消息。
- 验证：80 项 Python 测试及实际 Tk 窗口操作通过。

## 0.1 — 本地搜索基线（2026-09-09）

Git 提交：`f9031a9`。保留新增按钮反馈修复、全量目录名称搜索和弹窗选择版本。
